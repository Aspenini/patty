<h1>Import Profile: <%= h(preview.name) %></h1>

<div class="panel">
  <p class="kv"><span class="k">Will be saved as</span> <code><%= final_id %>.pattyfile</code></p>
  <p class="kv"><span class="k">Program/service</span>
    <% if program = preview.program %><code><%= h(program) %></code><% else %>Caddy only<% end %>
  </p>

  <h2>Caddy snippet</h2>
  <pre class="snippet"><%= h(preview.caddy) %></pre>

  <form method="post" action="/import/confirm">
    <input type="hidden" name="csrf_token" value="<%= csrf %>">
    <input type="hidden" name="content" value="<%= h(content) %>">
    <input type="hidden" name="id" value="<%= h(final_id) %>">
    <button class="btn primary" type="submit">Import</button>
    <a class="btn" href="/import">Cancel</a>
  </form>
</div>
